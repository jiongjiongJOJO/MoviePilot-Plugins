# 不可躺自动抢红包插件

自动在不可躺站点获取并依次领取当前红包列表中的可用红包，支持 Cron 定时执行和保存配置后立即执行一次。

## 使用说明

1. 在 MoviePilot 站点管理中添加或配置 `www.tangpt.top`。
2. 启用插件并配置 Cron，例如 `0 2,23 * * *` 每天 2 点和 23 点各执行一次。
3. 需要立即执行时勾选“立即运行一次”并保存配置，任务完成后该选项会自动关闭。

插件会根据获取到的红包列表触发抢红包；当列表为空、达到站点每日领取上限或达到安全刷新轮数上限时结束任务。每个红包领取间隔 1 秒，避免请求过快。
